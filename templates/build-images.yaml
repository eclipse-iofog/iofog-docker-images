parameters:
  platform: ''

steps:
  - template: './docker-task.yaml'
    parameters:
      platform: $(platform)
      target: java
  - template: './docker-task.yaml'
    parameters:
      platform: $(platform)
      target: nodejs
  - template: './docker-task.yaml'
    parameters:
      platform: $(platform)
      target: python
  - template: './docker-task.yaml'
    parameters:
      platform: $(platform)
      target: python3
