parameters:
  platform: ''

steps:
  - template: './docker-task.yml'
    parameters:
      platform: $(platform)
      target: java
  - template: './docker-task.yml'
    parameters:
      platform: $(platform)
      target: nodejs
  - template: './docker-task.yml'
    parameters:
      platform: $(platform)
      target: python
  - template: './docker-task.yml'
    parameters:
      platform: $(platform)
      target: python3
